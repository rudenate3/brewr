import { blue } from '../variables/colors'
import { typeScale } from '../variables/typography'

export const defaultTheme = {
  cardBackground: blue[100],
  cardHeader: blue[200],
  headerTextColor: blue[300],
  typography: typeScale
}
